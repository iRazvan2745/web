services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.prod
    x-ports:
      - irazz.lol:8000/https
      - 3000/https
    environment:
      NODE_ENV: production
    restart: unless-stopped
